/* ============================================================
   docker-odoo TUI - Stylesheet
   Layout: Header / [Instances | Actions] / Progress* / Output / Footer
   ============================================================ */

Screen {
    background: $surface;
}

/* ----- Header / Footer ----- */
Header {
    dock: top;
}
Footer {
    dock: bottom;
}

/* ----- Main split: instances | actions ----- */
#main {
    height: 1fr;
    margin: 0 1;
}
#left, #right {
    width: 1fr;
    border: round $primary;
    padding: 0 1;
    margin: 0;
}
#left {
    /* Ligeramente mas ancho para que entren nombres largos como
       'demo-integration-core' sin truncar. */
    width: 55%;
    margin-right: 1;
    border: round $primary;
}
#right {
    border: round $secondary;
}

/* ----- Subtitles (encabezado de cada panel) ----- */
Static#subtitle {
    color: $secondary;
    text-style: bold;
    padding: 0 2;
    height: 1;
    background: $boost;
}
Static#subtitle_meta {
    color: $text-muted;
    padding: 0 2;
    height: 1;
}

/* ----- ListViews ----- */
ListView {
    height: 1fr;
    padding: 0 0;
}
ListView > ListItem {
    padding: 0 1;
}
ListView > ListItem.--highlight {
    background: $accent;
    text-style: bold;
}
OptionList > Option.--highlight {
    background: $accent;
    text-style: bold;
}

/* ----- Category headers en la lista de acciones ----- */
ListView > ListItem.category_header {
    padding: 0 1;
    color: $secondary;
    text-style: bold;
    background: $boost;
}
ListView > ListItem.category_header.--highlight {
    background: $accent;
    color: $text;
}

/* ----- Output log (RichLog) ----- */
RichLog {
    height: 30%;
    min-height: 5;
    border: round $primary;
    background: $surface-darken-1;
    margin: 0 1;
    padding: 0 1;
}

/* ----- Modal box ----- */
#modal_box {
    background: $panel;
    border: thick $accent;
    padding: 1 3;
    width: 60;
    height: auto;
    align: center middle;
}
#modal_box Label {
    width: 100%;
    content-align: center middle;
}
#modal_title {
    text-style: bold;
    color: $accent;
    padding-bottom: 1;
}
#modal_message {
    padding: 1 0;
}
#modal_fields Input {
    margin-bottom: 1;
}
.field_label {
    color: $secondary;
}
#modal_buttons {
    align-horizontal: center;
    height: 3;
    padding-top: 1;
}
#modal_buttons Button {
    margin: 0 1;
}

/* ----- Picker modal (fzf-style) ----- */
#picker_panes {
    height: 1fr;
    margin-top: 1;
}
#picker_left, #picker_right {
    width: 1fr;
    border: round $primary;
    padding: 1 2;
    margin: 0 1;
}
#picker_left OptionList, #picker_right OptionList {
    height: 1fr;
}
#picker_hints {
    color: $secondary;
    padding: 0 1;
}

/* ============================================================
   UpdateProgress widget
   ============================================================ */
#update_progress {
    display: none;
    border: round $success;
    background: $surface-darken-1;
    padding: 0 2;
    height: auto;
    min-height: 11;
    margin: 0 1 1 1;
}

#update_progress #up_title {
    text-style: bold;
    color: $success;
    padding: 0 1;
    height: 1;
}

#update_progress ProgressBar {
    width: 100%;
    margin: 0 1;
}

#update_progress #up_progress_label,
#update_progress #up_remaining {
    color: $text;
    padding: 0 1;
    height: 1;
}

#update_progress #up_progress_label {
    text-style: bold;
}

#update_progress #up_filters_row {
    height: 1;
    padding: 0 1;
    align: left middle;
}
#update_progress #up_filters_row Static {
    margin-right: 2;
}
#update_progress #up_filters_row Static.active {
    text-style: bold;
    color: $success;
}
#update_progress #up_filters_row Static.inactive {
    color: $text-muted;
}

#update_progress #up_hints_row {
    height: 1;
    padding: 0 1;
    color: $text-muted;
}
#update_progress #up_hints_row Static {
    margin-right: 2;
}
